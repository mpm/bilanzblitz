#!/usr/bin/env ruby
# frozen_string_literal: true

# SKR03 Mapper - Unified CLI for SKR03 to HGB Mapping
#
# This is the main entry point for the SKR03 mapping CLI tool.
# It loads all required modules and starts the CLI router.

# Add lib directory to load path
$LOAD_PATH.unshift(File.expand_path('../lib', __dir__))

# Require the top-level module
require 'contrib'

# Change to the directory containing the data files (default: current directory)
# This maintains compatibility with the original scripts
Dir.chdir(Dir.pwd)

# Start the CLI
Contrib::SKR03Mapping::CLI.start(ARGV)
